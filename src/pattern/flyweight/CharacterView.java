package pattern.flyweight;

import javafx.scene.image.Image;
import javafx.scene.image.ImageView;

public class CharacterView extends ImageView {
	public CharacterView(Image chr) {
		super(chr);
	}
}
